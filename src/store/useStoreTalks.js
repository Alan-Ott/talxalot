import { create } from 'zustand';

const useStoreTalks = create((set) => ({

    addTalk: (talk) =>
        set((state) => ({

        })),

    updateTalk: (talk) =>
        set((state) => ({

        })),

    deleteTalk: (talk) =>
        set((state) => ({

        })),

    listTalks: (talk) =>
        set((state) => ({

        })),
}));

export default useStoreTalks;